#ifndef STDIO
#define STDIO
#include <stdio.h>
#endif

int lists_stack_push(struct data *d);
int lists_stack_pop(struct data *d);
int lists_stack_is_empty(struct data *d);
int lists_stack_is_full(struct data *d);
